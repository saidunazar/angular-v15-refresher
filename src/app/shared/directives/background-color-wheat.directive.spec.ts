import { BackgroundColorWheatDirective } from './background-color-wheat.directive';

describe('BackgroundColorWheatDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundColorWheatDirective();
    expect(directive).toBeTruthy();
  });
});
